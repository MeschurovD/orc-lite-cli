# orc-lite-cli - План задач

## Фаза 1: Инициализация проекта

- [ ] **1.1** Инициализировать package.json, tsconfig.json, зависимости
- [ ] **1.2** Настроить ESM, пути, скрипты сборки
- [ ] **1.3** Создать базовую структуру директорий

## Фаза 2: Перенос ядра из orc-cli

- [ ] **2.1** Перенести и адаптировать `types.ts` - убрать UI-типы, адаптеры кроме opencode, добавить типы расписания
- [ ] **2.2** Перенести `core/config.ts` - переименовать файл конфига в `orc-lite.config.json`, расширить Zod-схему полями `schedule` и `daemon`
- [ ] **2.3** Перенести `services/git.ts` - без изменений
- [ ] **2.4** Перенести `services/logger.ts` - убрать EventBus
- [ ] **2.5** Перенести `services/notifier.ts` - Telegram + webhook + прокси
- [ ] **2.6** Перенести `adapters/opencode-adapter.ts` - единственный адаптер, убрать фабрику
- [ ] **2.7** Перенести `adapters/prompt-builder.ts`
- [ ] **2.8** Перенести `core/stages/*` - implement, verify, test
- [ ] **2.9** Перенести `core/task-runner.ts` - адаптировать под единственный адаптер
- [ ] **2.10** Перенести `core/orchestrator.ts` - убрать EventBus, UI-события, упростить

## Фаза 3: CLI команды (базовые)

- [ ] **3.1** Создать `src/index.ts` - точка входа Commander.js
- [ ] **3.2** Команда `run` - немедленный запуск (перенос + фильтрация задач без schedule)
- [ ] **3.3** Команда `init` - интерактивная инициализация конфига
- [ ] **3.4** Команда `status` - статус пайплайна
- [ ] **3.5** Команда `reset` - сброс задачи
- [ ] **3.6** Команда `logs` - просмотр логов
- [ ] **3.7** Команда `validate` - валидация конфига

## Фаза 4: Планировщик (новый функционал)

- [ ] **4.1** Реализовать `core/scheduler.ts`:
  - Парсинг schedule поля (ISO vs cron)
  - Регистрация задач в croner
  - Управление жизненным циклом jobs
  - Перечитывание конфига (reconciliation)
  - Graceful shutdown
- [ ] **4.2** Команда `daemon` - запуск фонового планировщика
  - Лог-файл для daemon режима
  - PID-файл для предотвращения дублей
  - Обработка сигналов
- [ ] **4.3** Команда `add` - добавление задачи в очередь
  - `--schedule "ISO date"` для одноразового запуска
  - `--cron "expression"` для повторяющегося
  - Создание файла задачи из шаблона или указание существующего
- [ ] **4.4** Команда `queue` - отображение очереди
  - Таблица: задача | статус | расписание | след. запуск
  - Цветовая индикация статусов
- [ ] **4.5** Команда `schedule` - управление расписанием
  - Изменить расписание задачи
  - `--clear` для удаления расписания

## Фаза 5: Интеграция и тестирование

- [ ] **5.1** End-to-end проверка: run-режим (немедленный запуск)
- [ ] **5.2** End-to-end проверка: daemon-режим (запуск по расписанию)
- [ ] **5.3** Проверка Telegram нотификаций
- [ ] **5.4** Проверка edge-кейсов:
  - Просроченная дата schedule (в прошлом)
  - Daemon перезапуск - подхват существующего состояния
  - Конфликты git merge во время ночного прогона
  - Несколько задач на одно время

## Фаза 6: Доработки

- [ ] **6.1** README.md с документацией
- [ ] **6.2** npm bin entry point для глобальной установки
- [ ] **6.3** Шаблон задачи для `orc-lite add`

---

## Приоритеты

**Критический путь**: 1.x → 2.x → 3.1 → 3.2 → 4.1 → 4.2

Минимально рабочая версия = фазы 1-3 + задачи 4.1 и 4.2.
Остальные команды (add, queue, schedule) - удобства поверх рабочего ядра.

## Открытые вопросы

1. **Повторяющиеся cron-задачи**: после выполнения задачи с cron-расписанием, статус должен сбрасываться обратно в `pending`? Или использовать отдельный статус `recurring`?
   > Предложение: для cron-задач не менять статус на done, а вести лог выполнений отдельно. Статус `pending` сохраняется для следующего запуска.

2. **Daemon + run конфликт**: что если daemon запущен и пользователь вызывает `orc-lite run`? Нужен lock-файл?
   > Предложение: да, lock-файл `.orc-lite/run.lock`. `run` и `daemon` проверяют его перед стартом.

3. **Таймзона**: cron-расписание в какой таймзоне? Локальная системная?
   > Предложение: локальная по умолчанию, опция `timezone` в конфиге для явного указания.

4. **Просроченные задачи**: если daemon запущен и время задачи уже прошло - запустить немедленно или пропустить?
   > Предложение: запустить немедленно если прошло не более 1 часа, иначе пропустить с предупреждением.
